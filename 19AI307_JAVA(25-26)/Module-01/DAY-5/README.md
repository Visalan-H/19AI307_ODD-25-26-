# Ex.No:1(E) STRINGS AND MATH FUNCTION

## AIM:

Use basic string operations and math functions to process and display results.

## ALGORITHM :

1. Start the program.
2. Declare a string and a number.
3. Perform a string operation (like getting its length).
4. Apply math functions (such as square root or power).
5. Display all results and end the program.

#### Developed By: Visalan H
#### Register Number: 212223240183

## PROGRAM:

### to implement a Strings and Math Function using Java

## SOURCE CODE:

```java
public class Main {
    public static void main(String[] args) {
        String text = "Hello";
        int length = text.length();

        double x = 16;
        double root = Math.sqrt(x);
        double power = Math.pow(3, 2);

        System.out.println("Length: " + length);
        System.out.println("Square Root: " + root);
        System.out.println("Power: " + power);
    }
}
```

## OUTPUT:

```
Length: 5
Square Root: 4.0
Power: 9.0
```

## RESULT:

Program executed successfully.
